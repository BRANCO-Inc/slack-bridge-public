# Slack Bridge

For initial setup or Windows startup repair, use `/slack-bridge-setup` or read
`.agents/skills/slack-bridge-setup/SKILL.md`. Ask for the missing non-secret setup
information and guide the user through local hidden token entry.

For Slack worker turns and other repository work, follow `AGENTS.md`.
